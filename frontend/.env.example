# SellerIQ Pro — Frontend Environment Variables
# Copy this file to .env and fill in values

# Backend API URL (only needed for production / Vercel deployment)
# Leave blank for local development (Vite proxy handles it)
# For production, set to your Render backend URL (no trailing slash)
# Example: https://selleriq-api.onrender.com
VITE_API_URL==
